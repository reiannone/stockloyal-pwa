# -----------------------
# Logs
# -----------------------
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# -----------------------
# Node / React (frontend)
# -----------------------
node_modules
dist
dist-ssr
*.local
.vite/

# -----------------------
# PHP (backend)
# -----------------------
vendor/                 # if you use Composer; commit composer.json/lock
# DO NOT ignore api/config.php (it has no secrets; keep it in repo)

# -----------------------
# Environment / Secrets
# -----------------------
.env
.env.local
.env.*.local
api/.env
api/.env.*             # local/prod envs stay out of git
*.pem                   # certs/keys (e.g., RDS CA if ever copied in repo)
*.key
*.crt

# If you want an empty tracked certs dir:
# /api/certs/*
# !/api/certs/.gitkeep

# -----------------------
# Editor / OS junk
# -----------------------
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
